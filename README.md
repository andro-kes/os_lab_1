# Лабораторная работа №1: Проверка чисел на простоту

## 📋 Описание задания

В файле записаны команды вида: «число<endline>». Дочерний процесс производит
проверку этого числа на простоту. Если число составное, то дочерний процесс пишет это число в стандартный поток вывода. Если число отрицательное или простое, то тогда дочерний и родительский процессы завершаются. Количество чисел может быть произвольным.

### 🎯 Цель работы
Изучение работы с процессами в операционной системе Unix/Linux, включая:
- Создание процессов с помощью `fork()`
- Межпроцессное взаимодействие через пайпы
- Синхронизация процессов с помощью `waitpid()`
- Проверка чисел на простоту

## 🔧 Технические требования

- **ОС**: Linux/Unix (или WSL на Windows)
- **Компилятор**: GCC
- **Стандарт**: C99
- **Библиотеки**: math.h (для математических операций)

## 📁 Структура проекта

```
lab1/
├── src/               # Папка с исходниками
│   ├── main.c           # Основной исходный код
├── build/             # Директория для собранных файлов
├── numbers.txt        # Тестовый файл
├── Makefile           # Файл сборки
└── README.md          # Данная документация
```

## 🚀 Быстрый старт

### 1. Сборка проекта
```bash
make build
# или просто
make
```

### 2. Запуск программы
```bash
make run
```
Программа попросит ввести имя файла с числами.

## 📖 Подробное описание

### Алгоритм работы

1. **Чтение файла**: Программа читает имя файла из стандартного ввода
2. **Обработка чисел**: Для каждого числа в файле:
   - Создается новый дочерний процесс (`fork()`)
   - Создаются пайпы для передачи данных между процессами
   - Число передается дочернему процессу через пайп
   - Дочерний процесс проверяет число на простоту
   - Результат передается обратно родительскому процессу

3. **Логика завершения**:
   - **Составное число** → выводится в стандартный поток
   - **Простое число** → оба процесса завершаются
   - **Отрицательное число** → оба процесса завершаются

### Функции программы

#### `checkSimple(long long n)`
Проверяет число на простоту:
- Возвращает `1` для простых чисел
- Возвращает `0` для составных чисел  
- Возвращает `-1` для отрицательных чисел или 0/1

#### Основной процесс (`main()`)
- Читает файл с числами
- Создает дочерние процессы для каждого числа
- Управляет межпроцессным взаимодействием
- Выводит результаты

## 🛠️ Команды Makefile

| Команда | Описание |
|---------|----------|
| `make build` | Собрать программу |
| `make run` | Запустить программу (интерактивно) |
| `make clean` | Очистить собранные файлы |
| `make all` | Псевдоним для `make build` |

## 📝 Формат входного файла

Создайте файл с числами, по одному на строку. Например, `numbers.txt`:
```
4
6
8
9
10
12
15
17
19
23
-5
2
3
5
7
```

Затем запустите программу и введите имя файла.

## 🔍 Пример работы

### Входные данные (numbers.txt):
```
4
6
8
9
10
12
15
17
```

### Команды:
```bash
make build
make run
# Введите: numbers.txt
```

### Ожидаемый вывод:
```
Parent created child process 12345 for number: 4
Child process 12345 processing number: 4
4
Parent created child process 12346 for number: 6
Child process 12346 processing number: 6
6
Parent created child process 12347 for number: 8
Child process 12347 processing number: 8
8
Parent created child process 12348 for number: 9
Child process 12348 processing number: 9
9
Parent created child process 12349 for number: 10
Child process 12349 processing number: 10
10
Parent created child process 12350 for number: 12
Child process 12350 processing number: 12
12
Parent created child process 12351 for number: 15
Child process 12351 processing number: 15
15
Parent created child process 12352 for number: 17
Child process 12352 processing number: 17
Number 17 is prime or negative. Terminating both processes.
Child process terminated. Parent also terminating.
```

## 🐛 Отладка

### Использование GDB
```bash
make build
gdb build/prime_checker
```

## ⚠️ Известные ограничения

1. **Производительность**: Алгоритм проверки простоты не оптимизирован для больших чисел
2. **Память**: Каждый дочерний процесс создает свои пайпы
3. **Платформа**: Работает только на Unix/Linux системах

## 🔧 Настройка окружения

### Для WSL (Windows Subsystem for Linux)
```bash
# Установка необходимых пакетов
sudo apt update
sudo apt install build-essential gcc make

# Проверка установки
gcc --version
make --version
```

### Для Ubuntu/Debian
```bash
sudo apt install build-essential
```

### Для CentOS/RHEL
```bash
sudo yum groupinstall "Development Tools"
```

## 📚 Теоретическая часть

### Системные вызовы

- **`fork()`**: Создает новый процесс как копию текущего
- **`pipe()`**: Создает пайп для межпроцессного взаимодействия
- **`read()`/`write()`**: Чтение/запись данных через файловые дескрипторы
- **`waitpid()`**: Ожидание завершения дочернего процесса
- **`close()`**: Закрытие файлового дескриптора

### Межпроцессное взаимодействие

Программа использует два пайпа:
- **pd** (parent → child): Передача числа от родителя к дочернему процессу
- **cd** (child → parent): Передача результата от дочернего процесса к родителю

## 👥 Автор

**Студент**: Денисов Андрей
**Группа**: М8О-203БВ-24  
**Дата**: 28.09.2025 

## 📄 Лицензия

Данный проект создан в учебных целях для изучения системного программирования.